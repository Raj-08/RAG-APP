The app uses Chainlit for creating a front end for the chat bot, Llama2 for creating embeddings and generation and FAISS for vector storage. 


pip install -r requirements.txt

Usage - 

chainlit run app.py -w 
